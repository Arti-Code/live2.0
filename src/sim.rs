/* use macroquad::


pub struct Simulation {
    pub ui_state: UIState,
    pub signals: Signals,
    pub main_timer: Timer,
    pub sel_time: f32,
    pub selected: u8,
    pub mouse_state: MouseState,
} */